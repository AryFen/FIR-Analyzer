RoughDataCleaning.py reduced the files in 'Datasets' to a single, usable dataset.
FinalData.csv is this reduced dataset.
Forecaster.py uses an LSTM to predict Food Insecurity Rates. Currently, it is set up for testing in 2021.
app.py is a near-complete dashboard in Streamlit (the most interesting features are already implemented).
Currently working on a JavaScript implementation with Leaflet.js.
